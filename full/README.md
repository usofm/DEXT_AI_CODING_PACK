# Full AI Memory Archive

The root files are compact/curated agent references. This directory preserves the complete generated artifacts in numbered parts so GitHub connector size limits do not truncate them.

Load parts in numeric order when reconstructing the full artifact.

## Full memory

`DEXT_AI_MEMORY_ENRICHED` is split into:

```text
DEXT_AI_MEMORY_ENRICHED_PART_01.md
...
DEXT_AI_MEMORY_ENRICHED_PART_09.md
```

## Full API symbol index

`DEXT_API_SYMBOL_INDEX` is split into:

```text
DEXT_API_SYMBOL_INDEX_PART_01.md
...
DEXT_API_SYMBOL_INDEX_PART_04.md
```

For normal AI coding, prefer selective loading: Decision Tree + Anti-Patterns always, then Symbol/Example/Memory parts on demand.
